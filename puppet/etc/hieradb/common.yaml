classes:
  - apt
  - locales
  - ntp
  - timezone
  - postfix

apt::always_apt_update: true
apt::disable_keys:      true

locales::locales:
  - 'en_AU.UTF-8 UTF-8'
  - 'en_US.UTF-8 UTF-8'
  - 'en_GB.UTF-8 UTF-8'

timezone::timezone: 'Australia/Sydney'

ntp:servers:
  - 'ntp.ubuntu.com'
